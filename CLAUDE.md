# Claude Project Instructions

When interacting with Claude for this project:

- Focus on real-world business value over toy examples.
- Do not add real CRM credentials or secrets to the code.
- Provide comprehensive test coverage for new backend logic using pytest.
- Maintain a clean Git history and atomic commits (when applicable).
- Keep UI components functional and styled cleanly using Tailwind CSS (avoid unnecessary generic colors).
